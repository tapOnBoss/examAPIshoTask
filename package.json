{
  "name": "examapishotask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "modules": "^0.4.0",
    "mongoose": "^6.8.4"
  }
}

/** Documentation
 * @route POST /to-do
 * @group To-do - Operations about to-do
 * @param {string} title.body.required - The title of the to-do
 * @param {string} description.body.required - The description of the to-do
 * @returns {object} 201 - To-do created
 * @returns {Error}  default - Unexpected error
 */
