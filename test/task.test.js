const request = require('supertest');
const app = require('../app');

describe('Tasks', () => {
    it('should retrieve tasks for a user', (done) => {
    // test task retrieval
});

it('should create a task for a user', (done) => {
    // test task creation
});
});
